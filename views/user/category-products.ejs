<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= category.name %> - Shop</title>

  <!-- BOOTSTRAP LINK -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-FWb8A+9U1sqVybjF4bOadftYBn6n3qOdR0xqE5D5kQ0Xq9nJk7r6Yl1FgG2ooM4j" crossorigin="anonymous">

  <style>
    .breadcrumb a {
      color: #007bff;
      text-decoration: none;
    }
    .breadcrumb span {
      margin: 0 5px;
      color: #6c757d;
    }
  </style>

</head>
<body>

  <main class="main">
    <div class="page-header breadcrumb-wrap">
      <div class="container">
        <div class="breadcrumb">
          <a href="/shop" rel="nofollow">Shop</a>
          <span></span> 
          <span><%= category.name %></span>
        </div>
      </div>
    </div>
  
    <section class="mt-50 mb-50">
      <div class="container">
        <h2 class="text-center mb-5" style="font-weight: bold;"><%= category.name %> Collection</h2>
  
        <div class="row g-4">
          <% if (products.length === 0) { %>
            <div class="col-12 text-center">
              <h4>No products found in this category.</h4>
            </div>
          <% } else { %>
            <% products.forEach(product => { %>
              <div class="col-lg-4 col-md-6 col-sm-6">
                <div class="card h-100 shadow-sm border-0" style="display: flex; flex-direction: column; align-items: center;">
              
                  <div style="width: 100%; height: 250px; background: #f8f9fa; display: flex; align-items: center; justify-content: center; overflow: hidden;">
                    <img src="/uploads/re-image/<%= product.productImage[0] %>" alt="<%= product.productName %>" style="max-height: 100%; max-width: 100%; object-fit: contain;">
                  </div>
                
                  
                  <div class="card-body text-center px-3 py-2" style="width: 100%;"> 
                    <h5 class="card-title mb-1" style="font-size: 16px; font-weight: 600;"><%= product.productName %></h5>
                    <p class="card-text text-success mb-1" style="font-size: 15px;">₹ <%= product.salesPrice %></p>
                    <% if (product.regularPrice && product.regularPrice > product.salesPrice) { %>
                      <p class="card-text text-muted text-decoration-line-through mb-2" style="font-size: 13px;">₹ <%= product.regularPrice %></p>
                    <% } %>
                    <a href="/productDetails?id=<%= product._id %>" class="btn btn-primary btn-sm">View Product</a>
                  </div>

                </div>
               
                </div>
              </div>
            <% }) %>
          <% } %>
        </div>
      </div>
    </section>
  </main>
  
</body>
</html>




